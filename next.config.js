// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Habilita React Strict Mode para detectar errores
  swcMinify: true, // Habilita el minificador SWC para mejorar el rendimiento
};

module.exports = nextConfig;

